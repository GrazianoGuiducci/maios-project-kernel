# MAIOS Project Kernel current state

updated: 2026-09-01
active_surface: autonomous self-configuring `maios-project-kernel` 3.0.2 release
status: the bounded SSK functional delta is implemented in living source,
passes the source behavioral suite, is represented in the deterministic
tracked `package/` and is published on `main` with immutable tag and GitHub
Release `v3.0.2`; site and runtime effects remain separate

## Current resultant

The product remains an installable, self-configuring Project Kernel for a new
or existing target. Version 3.0.2 does not copy TM9 identity, private topology,
temporal state, authority or a complete source Kernel. It translates the
result-changing functions into the smallest product-native owners:

- entry is adaptive and discretionary: an already capable coder begins
  directly when target, intended change, effect and recovery are sufficiently
  clear, and expands explanation only when a missing relation changes them;
- `maios.resultant-readback.v3` preserves one compact causal margin and can
  update every genuinely open front while keeping one explicit focus;
- one resultant can carry zero or more distinct entries in `learning_deltas`,
  each bound to its closest owner and activation relation, in one atomic
  project-local transition;
- optional semantic sensitivity inside the preprojection readback can preserve,
  correct or refuse collapse when a change of meaning is material, without
  creating a universal vocabulary gate;
- one optional `integration_handoff` may enter the reversible configuration
  owner when it helps the first project movement; installer plans and receipts
  remain purely technical and never carry transcript or credentials.

`maios.operating-state.v2` owns the causal margin, open fronts, focus, result
history and reachable learning. Current operator intent and freshly verified
project reality continue to outrank stored continuity. `START_HERE.md` remains
the stable boot and does not become a mutable conversation summary.

The public knowledge and contribution field remains repository-native through
`knowledge/KERNEL.md`, `maios-kernel-study`, `maios-kernel-contribution` and
`contributions/`. The ChatGPT-deposited Paper-specific research field and
`maios-kernel-paper` owner are removed temporarily. This does not publish,
withdraw or decide a later Paper movement outside this repository source.

The reusable kernel-to-external-package passage is preserved in the owning TM9
competence: distinguish source function from current source topology, translate
only the causal difference into the receiving product owner, retain unknown and
unrepresented possibilities, prove only the state actually observed, and let
neutral product learning improve the method without turning this package into
a universal model.

## Proof state

- the shared Project Kernel family remains `3.0.0`; product patch identity is
  `3.0.2`;
- the canonical family contract remains exact with the autonomous startup
  relation `required`; `kernel/AUTONOMOUS_ENTRY_CONTRACT.json` separately owns
  the product's `discretionary` entry policy and the family synchronizer
  validates both without changing the Form lane;
- the historical RepoKernel source receipt remains immutable and records its
  earlier required interview relation; the MAIOS owner-native entry profile
  deliberately translates it through the product-owned contract;
- 31 local tests pass after the source delta, including plural closest-owner
  learning, causal-margin reentry, preservation of non-focused open fronts,
  actual front removal and focus clearing, coupled semantic sensitivity,
  positive and absent integration handoff, installation, runtime-cache
  recovery, host coverage and repository/package boundary checks;
- Python compilation and all changed JSON contracts validate locally;
- the tracked 3.0.2 package contains 55 files, 44 under `payload/`, passes
  source-to-distribution verification and reproduced identical manifest and
  inventory bytes across two consecutive builds;
- a fresh temporary Codex-target installation validates with discretionary
  entry, `maios.operating-state.v2`, a present causal margin and no inherited
  fronts or effect authority; the temporary target was removed after proof;
- an existing-project fixture executes the installed runtime, proves that the
  normal launcher creates no bytecode cache, introduces a real matching cache
  as a recovery counterexample, then uninstalls back to the original
  project-owned marker only;
- a real interactive person-coder direct entry and conditional expanded entry
  remain to be observed;
- release commit `62a095fbb84fa9c23855080bc492838abbae6761`, annotated tag
  `v3.0.2` and the matching GitHub Release are public; the Release has no
  attached assets because tracked `package/` is the installable projection;
- immutable `v3.0.1` history remains unchanged; public page, Form/RepoKernel
  lane, runtime activation and workstation recovery remain separate movements.

## Living owners

- `AGENTS.md`
- `skills/maios-project-integration/SKILL.md`
- `skills/maios-project-system/SKILL.md`
- `skills/maios-start-new-project/SKILL.md`
- `skills/maios-start-existing-project/SKILL.md`
- `skills/maios-project-context/SKILL.md`
- `skills/maios-project-competence-formation/SKILL.md`
- `skills/maios-project-host-adaptation/SKILL.md`
- `knowledge/KERNEL.md`
- `skills/maios-kernel-study/SKILL.md`
- `skills/maios-kernel-contribution/SKILL.md`
- `contributions/`
- `setup/MAIOS_SETUP_CONTRACT.md`
- `state/OPERATING_STATE.template.json`
- `schemas/RESULTANT_READBACK.schema.json`
- `kernel/`, including the product-owned `AUTONOMOUS_ENTRY_CONTRACT.json`
- `src/maios_project_kernel/`
- `release/PROJECTION.json`

`package/` is the generated installable projection, not a second living owner.

## Boundary and next movement

current_next: inspect and reconcile the documentation that each supported host
uses to understand, discover and activate the 3.0.2 Kernel, then enter the
operator-selected, separately owned Form-generated package movement; keep site,
runtime, real interactive acceptance and total workstation recovery distinct

supersession_condition: replace this state when the deterministic 3.0.2
package identity changes, host-documentation review changes an owner-native
entry, a source failure changes the selected delta, or a later real interactive
entry supplies new behavioral evidence

validation_deferred: real person-coder direct entry, conditional expanded
entry, later non-identical assimilation, public page and total workstation
recovery; the Form package is the next selected product object, not part of
this autonomous proof

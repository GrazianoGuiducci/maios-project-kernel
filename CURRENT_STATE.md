# MAIOS Project Kernel current state

updated: 2026-09-01
active_surface: autonomous self-configuring `maios-project-kernel` 3.0.3 release snapshot
status: the exact GPT Pro review of immutable `v3.0.2` has produced a bounded
autonomous stabilization delta in living source and tracked `package/`; the
operator has selected one exact 3.0.3 repository commit, push, annotated tag
and GitHub Release movement, whose public identity remains to be observed

## Current resultant

Version 3.0.3 preserves the complete 3.0.2 functional transfer: discretionary
coder entry, compact causal reentry with open fronts, plural closest-owner
learning, semantic preprojection sensitivity, optional integration handoff and
conservative project-local recovery. It adds four owner-native corrections:

- the builder rejects an unsupported
  `AUTONOMOUS_ENTRY_CONTRACT.contract_version` before projection;
- installed `maios.py status` validates the source manifest and rejects an
  unsupported entry-contract version or product-version drift between
  `.maios/SOURCE_MANIFEST.json` and the autonomous entry contract;
- coder-facing integration distinguishes idempotent reapplication of the exact
  same artifact from unsupported in-place migration between product versions;
- resultant documentation now matches the implementation: each JSON file is
  replaced atomically and caught failures trigger rollback, but no crash-safe
  multi-file transaction or interprocess serialization is claimed.

The host documentation now points to current owner-qualified convention sources
for Codex, Claude Code, OpenCode, OpenClaw, Pi and Hermes. DSH remains a
portable `.agents/skills` projection without a package-bound public owner
source or native discovery claim. The Hermes guide states that project-local
`HERMES_HOME` selects the whole profile boundary, not only its skill folder.

The public study and contribution field remains repository-native through
`knowledge/KERNEL.md`, `maios-kernel-study`, `maios-kernel-contribution` and
`contributions/`. It remains outside the installable projection. The
Paper-specific research field and `maios-kernel-paper` owner remain temporarily
absent; this product state does not decide a later Paper movement elsewhere.

The repository remains the autonomous product and living source; tracked
`package/` remains its generated installable projection. The shared Project
Kernel family contract remains version `3.0.0` with autonomous startup relation
`required`. The separate product-owned autonomous entry contract remains the
owner of discretionary entry and now identifies product version `3.0.3`.
No Form context, RepoKernel source, private TM9 topology, temporal state,
credentials or external authority enters the package.

## Proof state

- the review findings were checked against exact release commit
  `62a095fbb84fa9c23855080bc492838abbae6761` and living main baseline
  `f301bcd83336ad37a0e9bfce1b9e5784af5c2865`;
- negative tests first reproduced builder acceptance of an unknown contract
  version and installed-runtime acceptance of contract/product identity drift;
- both losses are corrected and regression-locked;
- 33 local source and synthetic behavior tests pass;
- the family synchronizer validates documented product `3.0.3`, supported
  autonomous contract `1.0.0` and the unchanged family `3.0.0` as distinct
  exact axes;
- the deterministic 3.0.3 package contains 55 files, 44 under `payload/`, and
  passes exact source-to-distribution verification;
- current host paths were compared with owner documentation; this is source
  confirmation of conventions, not native installed-host behavior;
- an extracted package copy was exercised on Windows with Python 3.13.2: new
  installation, exact-artifact idempotent reapplication, runtime status and
  uninstall pass; all eight adapter projections also install, verify, execute
  and uninstall from existing-project fixtures while preserving the sole
  project-owned marker and leaving no bytecode cache;
- the exact 3.0.3 source snapshot is selected for repository publication; its
  public branch, annotated tag, GitHub Release and CI remain unverified until
  external readback completes;
- real person-coder entry, native host discovery/use, later non-identical
  assimilation, site and runtime remain separately unverified or unselected.

## Living owners

- `AGENTS.md`
- `README.md` and `README.it.md`
- `skills/maios-project-integration/SKILL.md`
- `skills/maios-project-system/SKILL.md`
- `skills/maios-start-new-project/SKILL.md`
- `skills/maios-start-existing-project/SKILL.md`
- `skills/maios-project-context/SKILL.md`
- `skills/maios-project-competence-formation/SKILL.md`
- `skills/maios-project-host-adaptation/SKILL.md`
- `knowledge/KERNEL.md`
- `skills/maios-kernel-study/SKILL.md`
- `skills/maios-kernel-contribution/SKILL.md`
- `contributions/`
- `setup/MAIOS_SETUP_CONTRACT.md`
- `state/OPERATING_STATE.template.json`
- `schemas/RESULTANT_READBACK.schema.json`
- `templates/project/HOSTS.md`
- `docs/COMPATIBILITY.md`
- `kernel/`, including `AUTONOMOUS_ENTRY_CONTRACT.json`
- `src/maios_project_kernel/`
- `release/PROJECTION.json`

`package/` is the generated installable projection, not a second living owner.

## Boundary and next movement

current_next: perform the selected 3.0.3 repository commit, authenticated push,
annotated tag and GitHub Release; then observe the exact public refs, Release
object and CI before recording the publication receipt and entering the
separately owned Form-generated package movement

supersession_condition: replace this state when final verification changes the
snapshot, the publication effect establishes an immutable 3.0.3 identity, or a
real installed-host result changes the behavioral proof state

validation_deferred: real person-coder direct entry, conditional expanded
entry, native host discovery and use, later non-identical assimilation, site,
runtime and workstation recovery; Form/RepoKernel code and bytes remain
unchanged by this autonomous stabilization

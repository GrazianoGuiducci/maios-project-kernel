# MAIOS Project Kernel 2.0.0 — generated distribution

Use `install.py` and follow `INSTALL.md`. `payload/` is the reviewed project
tree; do not copy its files manually. `MANIFEST.json` and
`PACKAGE_INVENTORY.json` bind the exact artifact, while the installer produces
an exact target plan and recovery-bearing receipt.

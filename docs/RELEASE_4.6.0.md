# MAIOS Project Kernel 4.6.0 — superseded development candidate

Historical development record. The current product is described in
[5.0.0 release content](RELEASE_5.0.0.md); statements below describe the earlier
4.6.0 candidate and its proof scope.

Status: prepared for final release review, not published.

The candidate brings current understanding, open possibilities, acquired
knowledge and source-grounded expression into reachable general competences.
Learning from demonstrations enters formation on demand. Existing system,
context, coordination and continuity owners retain responsibility for the work.

FDLA now reconstitutes an earlier field through a later consequence while
preserving the event, the understanding then and the understanding now.
Material changes require renewed convergence of affected owners, consumers,
discovery, state and proof. Structural availability does not establish semantic
assimilation; independent-looking agreement is not independent evidence when
it shares one source of error.

Runtime corrections preserve explicit empty next relations, remove reopened or
preserved possibilities from current elimination, and bind operating state to
its latest terminal receipt. Current integrity follows the frontier and its
dependencies; `audit-continuum` separately reports cold genealogical damage.
History and recovery bytes remain in place. C01 requires no additional
provenance schema: receiver cases distinguish resolved context and explicit
empty selections using the current fields, including persisted intent source.

Production now directly maps current native owners into the package. The
current catalogue supplies profile identity; portable discovery is declarative.
Portable paths and collisions share one implementation. Invocation-owned
staging/backup and qualified cleanup preserve previous packages and foreign
work on failure. Official Actions use full immutable commit SHAs.

## Compatibility

Minor version 4.6.0; family 3.0.0 and the existing state, learning, receipt and
installation contracts remain unchanged. There is no state migration or new
generic controller. Explicit generated-plan consumption and `--source-only`
remain available; the latter is now an alias for the direct default. Maintainers
who intentionally need a historical generated result must select its plan and
hash explicitly. This is a deliberate build-default change, not a silently
relabelled historical selection. Already installed projects retain their local
knowledge and reconcile updates through UPDATE_CONTINUITY.

## Proof and release boundary

The candidate suite covers R01/R02/R04/R05/C02 and the C01 discriminant,
current catalogue and discovery, deterministic direct builds, portable paths,
failure recovery, installation, update continuity, host entries and active
documentation. CI runs the full suite, two builds and distribution verification
on Linux/Python 3.10 and Windows/Python 3.10 and 3.13, then retains a candidate
archive and checksum. The final source-bound return records actual outcomes,
commit, run and archive identities; this document does not prefill their success.

No protected version tag, immutable Release, Form or site effect is selected.
The candidate returns for final release review. Test fixtures establish their
stated software contracts; receiving-model behavior and maintained assimilation
need their own observations.

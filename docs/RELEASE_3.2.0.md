# MAIOS Project Kernel 3.2.0

This minor release adds operative coordination and shared-project continuation
competences to the self-installing package. It promotes the reviewed
`3.1.3+coordination.2` source at `925ea8a4eee7188063adfbe649d97a6d8b347ccb`.

- Situated coordination connects the existing delegation faculty to a living
  method: shared semantic context, instance identity, direct responsibility,
  cognitive freedom in delegation, returned effects and reusable learning.
- Host adaptation can understand and form a connection for another harness in
  the same project when an actual case makes it useful. The runtime retains its
  single selected adapter; installing this release does not configure another
  host or create a concurrent coordination service.
- Initial host observations and active competence admissions require their
  owner history. A first receipt cannot absorb pre-existing unrecorded claims.
  Incoherent files remain intact and recovery is visible before a write.
- Receipt and journal readers preserve equivalent root/path spellings until
  the shared confinement helper derives the relative path. Traversal, linked
  descendants and Windows junction confinement retain their existing checks.

Unknowns, harmless extensions, represented methods, living competence bodies
and legitimate historical replay remain available. The review closes with no
new blocking findings in the declared scope. Its evidence is source inspection
and exact-commit CI; real receiving-model use and later assimilation remain
separate, unobserved levels.

The suite contains 89 cases. Release CI runs Ubuntu/Python 3.10 and Windows with
Python 3.10 and 3.13, builds the package twice and verifies its tracked projection.
The package contains 62 files, including 51 payload files. The release publication
links the exact final commit and CI result; candidate evidence belongs to its
own commit and is not substituted for the final version check.

The shared family remains 3.0.0. Previous release tags remain unchanged. No
automatic cross-version migration is introduced. The installable projection is
the tracked `package/` directory. Form and website distribution retain their
separate publication state.
